openapi: 3.2.0
info:
  title: App/Lookup/Google Ads/Merchant Center API
  version: 4.1.0
  description: App/Lookup/GoogleAds/MerchantCenter
tags:
- name: App/Lookup/GoogleAds/MerchantCenter
  description: App/Lookup/GoogleAds/MerchantCenter
paths:
  /app/lookup/g/merchant-center:
    get:
      tags:
      - App/Lookup/GoogleAds/MerchantCenter
      operationId: appLookupGoogleAdsMerchantCenterQuery
      parameters:
      - name: linkingId
        in: query
        required: true
        schema:
          type: integer
      responses:
        '200':
          description: Successful
          content:
            application/json:
              schema:
                type: object
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBagSchema'
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorSchema'
        '403':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorSchema'
      summary: App lookup google ads merchant center query
      x-summary-source: derived
components:
  schemas:
    ErrorSchema:
      required:
      - errors
      properties:
        error:
          type: string
        message:
          type:
          - string
          - 'null'
      type: object
    ErrorBagSchema:
      required:
      - errors
      properties:
        errors:
          type: object
          additionalProperties:
            type: array
            items:
              type: string
      type: object
  securitySchemes:
    token:
      type: apiKey
      description: Bearer token authorization
      name: authorization
      in: header
